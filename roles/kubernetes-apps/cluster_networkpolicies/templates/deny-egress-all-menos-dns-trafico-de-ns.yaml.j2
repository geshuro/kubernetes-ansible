apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "{{ networkpolicies.egress.deny_all_menos_dns_trafico_ns.namespace }}-deny-egress-all-menos-dns-trafico-de-ns"
  namespace: {{ networkpolicies.egress.deny_all_menos_dns_trafico_ns.namespace }}
spec:
  policyTypes:
  - Egress
  podSelector: {}
  egress:
  # Permitir resolucion DNS
  - ports:
    - port: 53
      protocol: UDP
    - port: 53
      protocol: TCP